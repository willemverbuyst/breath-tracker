Little project to try out some more advanced React patterns and hooks.

```ts
import {
  createContext,
  useCallback,
  useEffect,
  useMemo,
  useRef,
  useState,
} from 'react'
```

```ts
const animation = useAnimation({ buttonRef }) // custom hook
```

![screenshot](./img/screenhot.png)
